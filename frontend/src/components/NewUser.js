//When user clicks create new user account, they should be rendered with this component

import React from 'react';

export default class NewUser extends React.Component{
    render(){
        return(
            <div>
                New User account creation page:
            </div>
        )
    }
}